{{#if this._lastValue}}
  {{#let (component this._lastValue) as |SvgJar|}}
    <SvgJar
      {{did-update this.updateSvg @name}}
      @title={{@title}}
      @titleId={{this.titleId}}
      aria-label={{@ariaLabel}}
      aria-hidden={{this.isAriaHidden}}
      aria-labelledby={{this.titleId}}
      ...attributes
    />
    {{!-- <Svg::Accessibility /> --}}
  {{/let}}
{{/if}}