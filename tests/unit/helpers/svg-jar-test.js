import { svgJar } from 'dummy/helpers/svg-jar';
import { module, test } from 'qunit';

module('Unit | Helper | svg jar');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = svgJar([42]);
  assert.ok(result);
});
